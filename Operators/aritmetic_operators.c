#include <stdio.h>

int main() {

    // % - maradékos osztás
    // augmented assigned oparators: x = x + 1 → x+=1

    int x = 5;
    int y = 2;

    float result = x / (float) y;

    printf("Result: %f\n", result);
}